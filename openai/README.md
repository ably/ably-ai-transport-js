# @ably/ai-transport/openai

This directory contains re-exports of `dist/openai`, allowing bundlers (such as Metro) which don't support the package.json `exports` field to resolve imports from `@ably/ai-transport/openai`.
