/**
 * SDK version used as the value of the `ai-transport-js` agent identifier
 * registered on the Realtime client's `options.agents` map for usage
 * tracking. Kept in sync with `package.json` by the `/release` workflow.
 */
export const VERSION = '0.1.0';
