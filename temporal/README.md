# @ably/ai-transport/temporal

This directory contains re-exports of `dist/temporal`, allowing bundlers (such as Metro) which don't support the package.json `exports` field to resolve imports from `@ably/ai-transport/temporal`.
