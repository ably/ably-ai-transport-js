# @ably/ai-transport/temporal/workflow

This directory contains re-exports of `dist/temporal/workflow`, allowing bundlers (such as Metro) which don't support the package.json `exports` field to resolve imports from `@ably/ai-transport/temporal/workflow`.
