# @ably/ai-transport/vercel

This directory contains re-exports of `dist/vercel`, allowing bundlers (such as Metro) which don't support the package.json `exports` field to resolve imports from `@ably/ai-transport/vercel`.
